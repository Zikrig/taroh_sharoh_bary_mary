# Section hints

`index.json` — каталог разделов отчётов (названия и порядок). По нему собираются PDF и бесплатная выдача в боте.

`hint_cards` в JSON больше не уходят в модель. В промпт передаётся полная натальная карта, а текст разделов задаётся в `services/report_prompts.py`.
